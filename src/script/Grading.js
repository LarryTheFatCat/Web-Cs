/**
 * @author  Tarkan Zarrouk
 * @param   None
 * @returns        Mark out of 2, if not 2/2 it will display what the student should / should've fixed inorder to get the 2/2
 * Main function, simply returns mark out of 2
 * 1: Full Name In File
 * 2: Submitted on time & / or before due date
 */
function autoGrader() {
    console.log("This is a test");
}